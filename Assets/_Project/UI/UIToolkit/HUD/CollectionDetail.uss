/* CollectionDetail — scheda dettaglio item */

.cdetail-root {
    position: relative;
    width: 400px;
    max-width: 94%;
    flex-shrink: 0;
    background-color: rgba(8, 12, 14, 0.97);
    border-width: 2px;
    border-color: rgba(181, 128, 209, 0.7);
    padding: 12px 14px 14px 14px;
    flex-direction: column;
    overflow: hidden;
}

.cdetail-corner {
    width: 3px;
    height: 3px;
    background-color: rgb(181, 128, 209);
}

/* Header */
.cdetail-header {
    flex-direction: row;
    align-items: flex-start;
    margin-bottom: 8px;
}

.cdetail-icon {
    width: 40px;
    height: 40px;
    min-width: 40px;
    min-height: 40px;
    -unity-background-scale-mode: scale-to-fit;
    margin-right: 8px;
}

/* Stesso linguaggio del Collection box: cornice / outliner viola */
.cdetail-icon-box {
    border-width: 2px;
    border-color: rgba(181, 128, 209, 0.7);
    background-color: rgba(12, 8, 18, 0.9);
    align-items: center;
    justify-content: center;
}

.cdetail-title-col {
    flex-direction: column;
    flex-grow: 1;
    flex-shrink: 1;
    min-width: 0;
}

.cdetail-name {
    font-size: 11px;
    color: rgb(200, 203, 200);
    -unity-font-style: bold;
    letter-spacing: 1px;
    white-space: normal;
}

.cdetail-typeid {
    font-size: 8px;
    color: rgba(181, 128, 209, 0.6);
    letter-spacing: 1px;
    margin-top: 2px;
}

/* Separator */
.cdetail-sep {
    height: 1px;
    background-color: rgba(181, 128, 209, 0.25);
    margin-top: 4px;
    margin-bottom: 6px;
}

/* Rows */
.cdetail-row {
    flex-direction: row;
    justify-content: flex-start;
    align-items: flex-start;
    margin-bottom: 5px;
}

.cdetail-key {
    font-size: 9px;
    color: rgba(192, 200, 197, 0.6);
    width: 118px;
    min-width: 118px;
    flex-shrink: 0;
    margin-right: 10px;
    -unity-text-align: middle-left;
}

.cdetail-value {
    font-size: 9px;
    color: rgb(192, 200, 197);
    flex-grow: 1;
    flex-shrink: 1;
    min-width: 0;
    -unity-text-align: upper-left;
}

.cdetail-value-wrap {
    white-space: normal;
    -unity-text-align: upper-left;
}

.cdetail-plh {
    color: rgba(192, 200, 197, 0.3);
}

.cdetail-hint {
    font-size: 8px;
    color: rgba(192, 200, 197, 0.35);
    -unity-text-align: middle-center;
    margin-top: 10px;
    letter-spacing: 1px;
    white-space: normal;
}
